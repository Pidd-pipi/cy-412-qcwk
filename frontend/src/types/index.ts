export type UserRole='resident'|'staff'|'admin'; export type RepairStatus='pending'|'assigned'|'processing'|'done'|'closed'; export type AnnouncementScope='all'|'building'|'unit'; export interface User{id:number;phone:string;nickname:string;avatar:string;role:UserRole;building:string;unit:string;room:string}; export interface Repair{id:number;user_id:number;title:string;description:string;type:string;images:string;status:RepairStatus;handler_id?:number;user?:User;handler?:User;rating:number;created_at:string;updated_at:string}; export interface Payment{id:number;user_id:number;fee_type:string;amount:number;month:string;status:string;paid_at?:string;created_at:string}; export interface Announcement{id:number;title:string;content:string;category:string;scope:AnnouncementScope;scope_building:string;scope_unit:string;publisher_id:number;publisher?:User;publish_at:string;top:boolean;read_count:number;confirmed_count:number;confirmed:boolean;unconfirmed_users:User[]}; export interface ApiResponse<T>{code:number;message:string;data:T}
