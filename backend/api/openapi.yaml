openapi: 3.0.3
info: { title: SmartEstate API, version: 1.0.0 }
paths:
  /healthz: { get: { summary: Health check } }
  /api/v1/auth/login: { post: { summary: Login } }
  /api/v1/repairs: { get: { summary: List repairs }, post: { summary: Create repair } }
  /api/v1/payments: { get: { summary: List payments }, post: { summary: Create bill } }
  /api/v1/announcements: { get: { summary: List announcements (residents: property scope only, staff: all) }, post: { summary: Publish announcement with scope all/building/unit } }
  /api/v1/announcements/{id}/confirm: { post: { summary: Confirm an urgent announcement (idempotent per resident) } }
