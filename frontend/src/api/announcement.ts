import {request} from '../utils/request';import type{Announcement,AnnouncementScope} from '../types';export const listAnnouncements=()=>request<Announcement[]>('/announcements');export const createAnnouncement=(data:{title:string;content:string;category:string;scope:AnnouncementScope;scope_building?:string;scope_unit?:string;top:boolean})=>request<Announcement>('/announcements',{method:'POST',body:JSON.stringify(data)});export const detailAnnouncement=(id:number)=>request<Announcement>(`/announcements/${id}`);export const confirmAnnouncement=(id:number)=>request<Announcement>(`/announcements/${id}/confirm`,{method:'POST'});
